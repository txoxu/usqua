<div class="bg-body-secondary shadow-lg">
  <div class="container">
    <div class="row">
      <h1><%= t('.title') %></h1>
      <p><%= t('.select') %></p>

      <%= link_to t('.next_tasting'), new_whiskey_tasting_path(@whiskey), class: 'btn btn-outline-primary' %>
      <%= link_to t('.whiskey_new'), new_whiskey_path, class: 'btn btn-outline-info' %>
      <%= link_to t('.whiskeys'), whiskeys_path, class: 'btn btn-outline-secondary' %>
    </div>
  </div>
</div>
