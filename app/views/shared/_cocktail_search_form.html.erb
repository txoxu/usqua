<%= form_with model: @cocktail_search_form, url: cocktails_path, method: :get, class: 'search-form text-center' do |f| %>
  <div class="row justify-content-center">
    <div class="col-md-6">
      <%= f.text_field :keyword, class: 'form-control', placeholder: 'キーワードを入力' %>
    </div>
    <div class="col-md-6">
      <%= f.submit t('helpers.submit.search'), class: 'btn btn-outline-primary' %>
    </div>
  </div>
<% end %>
